# Torbook

**This program no longer works properly due to Facebook disabling messages page in its mbasic version.**

Experiments with tor version of facebook via terminal interface. It is not quality code, but it works. Kind of.
